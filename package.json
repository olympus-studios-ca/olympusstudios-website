{
  "name": "olympus-studios-ca",
  "version": "1.0.0",
  "description": "Olympus Studios — Where creativity meets intelligent production.",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "preview": "eleventy --serve --port=8080",
    "clean": "rimraf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "playwright": "^1.62.1",
    "rimraf": "^6.0.0"
  },
  "dependencies": {
    "@11ty/eleventy-img": "^6.0.0",
    "js-yaml": "^4.1.0"
  }
}
